{
  "name": "pita",
  "version": "0.3.3",
  "private": true,
  "description": "Slack-compatible copy converter for Notion and Google Docs",
  "homepage": "https://github.com/ktym4a/pita#readme",
  "bugs": {
    "url": "https://github.com/ktym4a/pita/issues"
  },
  "license": "MIT",
  "author": "ktym4a",
  "repository": {
    "type": "git",
    "url": "https://github.com/ktym4a/pita.git"
  },
  "type": "module",
  "scripts": {
    "build": "wxt build",
    "changeset": "changeset",
    "check": "pnpm lint && pnpm fmt",
    "check:fix": "pnpm lint:fix && pnpm fmt:fix",
    "dev": "wxt",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint --type-aware --type-check .",
    "lint:fix": "oxlint --type-aware --type-check --fix .",
    "postinstall": "wxt prepare",
    "release": "git tag pita@$(node -p \"require('./package.json').version\") 2>/dev/null || true",
    "test": "vitest",
    "test:run": "vitest run",
    "version": "changeset version",
    "zip": "wxt zip"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.6.0",
    "@changesets/cli": "^2.30.0",
    "@vitest/coverage-v8": "^4.1.0",
    "jsdom": "^29.0.1",
    "oxfmt": "^0.41.0",
    "oxlint": "^1.56.0",
    "oxlint-tsgolint": "^0.17.1",
    "vitest": "^4.1.0",
    "wxt": "^0.20.20"
  },
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  }
}
